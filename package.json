{
  "name": "escrow",
  "version": "1.0.0",
  "type": "module",
  "description": "",
  "main": "index.js",
  "scripts": {
    "compile": "hardhat compile",
    "clean": "hardhat clean",
    "deploy:local": "node scripts/deploy.js",
    "frontend:build": "npm --prefix frontend run build",
    "frontend:install": "npm --prefix frontend install",
    "test": "echo \"Add scenario tests for the new escrow hub before relying on this in production.\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@openzeppelin/contracts": "^5.0.0",
    "hardhat": "^3.3.0"
  },
  "dependencies": {
    "ethers": "^6.16.0"
  }
}
